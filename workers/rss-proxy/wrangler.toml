name = "macro-intel-rss-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

[vars]
CACHE_TTL = "600"

# API keys — set as secrets via `npx wrangler secret put <KEY_NAME>`
# FRED_API_KEY = (set via wrangler secret put FRED_API_KEY)
# FINNHUB_KEY = (set via wrangler secret put FINNHUB_KEY)
# MARKETAUX_KEY = (set via wrangler secret put MARKETAUX_KEY)
